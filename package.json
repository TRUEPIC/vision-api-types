{
  "name": "vision-api-types",
  "version": "1.4.0",
  "description": "Vision API Types",
  "license": "UNLICENSED",
  "repository": "TRUEPIC/vision-api-types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/types && cp -r types/* dist/types && tsc",
    "clean": "tsc --build --clean"
  },
  "type": "module"
}
