The `enums` folder contains all of our Typescript enums that we'd like to use as
values in our code. These are separated because enums need to be `*.ts` instead
of `*.d.ts` files to be able to use them at runtime.
