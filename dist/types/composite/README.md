The `src/types/composite` folder contains internal types built for use in this
codebase. They usually compose of multiple types that are combined, extended,
picked, and omitted for common use cases in the codebase.

Use the `@internal` intellisense flag to hide these types from the generated
documentation.
