The `src/types/api` folder contains all of the request/response bodies and query
parameters used with the Vision API.

The type definitions in this folder will be automatically converted to OpenAPI
definitions for the Vision API documentation.

These are the type definitions we plan to publish for API consumers to use in
their own Typescript code.
